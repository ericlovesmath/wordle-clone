{
    "name": "wordle-clone",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "clean": "rm -rf build",
        "preview": "vite preview",
        "deploy": "npm run build && gh-pages -d build"
    },
    "devDependencies": {
        "typescript": "^4.5.4",
        "vite": "^2.8.0"
    },
    "dependencies": {
        "gh-pages": "^3.2.3"
    }
}
